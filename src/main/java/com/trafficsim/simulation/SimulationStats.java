package com.trafficsim.simulation;

/**
 * TODO: whatever live metrics you want the stats overlay to show — ideas: active vehicle count,
 * average speed, per-road queue length, throughput (completed trips per minute), average travel
 * time. A trailing time window (rather than an all-time total) will make throughput reflect
 * current conditions rather than the whole run.
 */
public class SimulationStats {
}
